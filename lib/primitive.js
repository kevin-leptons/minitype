'use strict'

/**
 * A heximal string which is follow the pattern `^0x[a-fA-F0-9]+$`.
 *
 * @typedef {string} Heximal
 */

/**
 * A heximal string which is follow the pattern `^0x[a-fA-F0-9]*$`.
 * `0x` represents for an empty byte array.
 *
 * @typedef {string} HeximalByteArray
 */

/**
 * A decimal string, has at least a decimal digit.
 *
 * @typedef {string} Decimal
 */

module.exports = {}
